# frozen_string_literal: true

require "bundler/gem_tasks"
task default: %i[]

namespace :git do
  desc "wwfwf"
  task :envio do
    
    # `git add . && git commit -m 'teste' && git push`
    sh 'yarn'
    # te = gets
    # puts te
  end
end